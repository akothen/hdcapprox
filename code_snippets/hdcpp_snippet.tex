hypervector<617> input_features = ...; 
hypermatrix<2048, 617> rp_matrix = ...;
hypermatrix<26, 2048> clusters_matrix = ...;
hypervector<2048> encoded_data = __hetero_hdc_matmul(input_features, rp_matrix);
hypervector<26> dists = __hetero_hdc_hamming_distance(encoded_data, clusters_matrix);
// Calculate hamming distance in sub-range [0 , 1024)
__hetero_hdc_red_perf( dists , 0.0, 0.5 , 1) ;
int label = __hetero_hdc_arg_min(dists);